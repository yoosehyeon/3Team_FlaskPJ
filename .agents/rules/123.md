---
trigger: always_on
---

ㅇㅇ
kweonww.
오프라인 표시
에서:#📏-규칙 app.py
#📏-규칙 채널의 시작이에요. 커뮤니케이션 규칙

1. 상대방 의사 존중
2. 감정 싸움 금지
3. 비방글 금지
유세현
 님이 Team Project GitHub 용어 스레드를 시작하셨어요(스레드 모두 보기). — 2026-03-16 오전 11:44
Team Project GitHub 용어
메시지 1개 ›
스레드에 새 메시지가 없어요.
10일 전
sssnyyyn
 님이 vscode 오류 줄이기 위함 스레드를 시작하셨어요(스레드 모두 보기). — 2026-03-24 오후 4:04
vscode 오류 줄이기 위함
메시지 1개 ›
스레드에 새 메시지가 없어요.
1일 전
이동규
 님이 Custom Instructions(Rules) 스레드를 시작하셨어요(스레드 모두 보기). — 2026-03-24 오후 10:45
Custom Instructions(Rules)
메시지 2개 ›
이동규
첨부 파일을 보려면 클릭하세요
6시간 전
이동규
 님이 :arrows_counterclockwise: Antigravity 에이전트 최적화 워크플로우 (Scenario) 스레드를 시작하셨어요(스레드 모두 보기). — 2026-03-24 오후 10:46
:arrows_counterclockwise: Antigravity 에이전트 최적화 워크플로우 (Scenario)
메시지 2개 ›
이동규
4시간 전
﻿
이동규 — 2026-03-24 오후 10:45
[Modu-Gil] Antigravity 에이전트 통합 Rules (v2.0 - 최종본)
팀원 여러분, 6인 협업 및 4일 스프린트 성공을 위해 아래 규칙을 Antigravity 에이전트에 반드시 적용해 주세요.

📂 [Structure] 프로젝트 구조 및 담당 영역
영역 준수: 본인 배정된 기능 폴더(src/components/[A-F]-category/) 외의 파일 수정은 원칙적으로 금지한다.

공통 파일 보호: app.py(App Factory), models.py, static/css/common.css 등 공통 설정 파일 수정 시 반드시 사용자에게 변경 사유를 브리핑하고 승인받아야 한다.

Jinja2 활용: 프론트엔드는 React가 아닌 Vanilla JS + Jinja2 기반이다. 에이전트는 .html 템플릿 내의 Jinja2 문법을 정확히 이해하고 작성한다.

🛡️ [Git & Conflict] GitHub 충돌 방지 수칙
Minimal Intervention (최소 수정): 기존에 작동하는 타인의 함수나 로직을 리팩토링하지 않는다. '기능 추가'와 '함수 단위 확장'을 우선한다.

Atomic Work (단위 작업): 한 번의 실행으로 하나의 논리적 기능(예: 특정 API 엔드포인트 1개, 버튼 UI 1개)만 구현하며, 여러 파일을 일괄 수정하지 않는다.

Line-based Edit: 파일 전체를 다시 쓰지 말고, 요청한 특정 라인이나 코드 블록만 정밀하게 수정하여 git diff 발생을 최소화한다.

Pre-Check: 코드를 생성하기 전, 현재 파일이 다른 팀원이 수정 중인 파일인지(파일명 접두어 확인 등) 자문하고 충돌 가능성을 보고한다.

💻 [Tech Stack] 상세 기술 명세 (엄수)
Backend: Python 3.10+ / Flask (App Factory Pattern).

Frontend: Vanilla JS / HTML5 / CSS3 / Jinja2 (React 사용 절대 금지).

ORM/DB: SQLAlchemy 사용. 로컬은 MariaDB, 서버는 Supabase(PostgreSQL)를 사용하므로 범용 SQL 문법 및 Alembic 마이그레이션을 준수한다.

Maps API: Kakao Maps 및 Tmap API(보행자 경로) 연동 로직 우선.

Real-time: Flask 내장 스트리밍을 활용한 SSE(Server-Sent Events) 구현.

Scheduling: Flask-APScheduler로 실시간 데이터(엘리베이터 등)를 동기화한다.

🚀 [Workflow] 에이전트 작업 절차
PRD 분석: 작업 전 PRD v13.0의 해당 기능 Acceptance Criteria를 확인한다.

스키마 확인: DBeaver DDL을 참고하여 데이터 타입을 정확히 맞춘다.

환경변수: 모든 API 키와 URL은 .env 변수를 사용하며 절대 하드코딩하지 않는다.

자가 검증: 코드 작성 후 터미널에서 서버를 실행하여 에러 여부를 스스로 체크한다.
이동규 — 오전 7:31
# Modu-Gil: 수원시 휠체어 통합 내비게이션 - AI Collaboration Rules

당신은 '수원시 휠체어 통합 내비게이션' 프로젝트의 시니어 풀스택 개발자입니다. 
다음의 PRD v3.0 규정을 엄격히 준수하여 코드를 생성하고 가이드하십시오.

## 1. Core Tech Stack & Infrastructure
- **Frontend**: React 18 (Vite), TanStack Query v5, Zustand, Tailwind CSS, Kakao Maps SDK, Turf.js, Zod
- **Backend**: Flask 3.0, Gunicorn (gevent), SQLAlchemy 2.0 (NullPool), Flask-CORS, Flask-Limiter
- **Database**: Supabase (PostgreSQL + PostGIS + Realtime + Storage + Auth)
- **Deployment**: Netlify (FE) / Cloudtype (BE: Port 6543) / Supabase (DB)

## 2. Infrastructure & Security Rules (CRITICAL)
- **DB Connection**: 반드시 `poolclass=NullPool`과 `pool_pre_ping=True`를 사용하며, 포트는 `6543`을 기준으로 합니다. (PgBouncer 대응)
- **API Proxy**: Tmap, ODsay, 공공데이터 API는 반드시 Flask 백엔드를 통해서만 호출합니다. 클라이언트 직접 호출은 'Kakao Maps'와 'Supabase Anon'으로 제한합니다.
- **CORS Configuration**: `app.py`에서 `VITE_DOMAIN` 환경변수를 기반으로 한 동적 Origin 설정을 생성하십시오.
- **Environment Variables**: `VITE_` 접두사는 클라이언트 노출용(Kakao Key, Supabase Anon Key)에만 사용합니다. `SERVICE_ROLE_KEY`나 외부 API Key는 절대 포함하지 마십시오.

## 3. Backend Development Patterns (Flask)
- **Worker Class**: Gunicorn 설정 시 반드시 `--worker-class gevent`를 전제로 코드를 작성하십시오.
- **Realtime**: Flask SSE를 절대 사용하지 마십시오. 모든 실시간 기능은 Supabase Realtime으로 유도합니다.
- **Error Handling**: `structlog`와 `sentry_sdk`를 연동한 전역 에러 핸들러 형식을 유지하십시오.
- **Database Migration**: RLS(Row Level Security) 정책은 각 테이블의 DDL 파일(`0003_places.sql` 등) 내부에 통합하여 작성하십시오.

## 4. Frontend Development Patterns (React)
- **State Management**: 
  - 서버 데이터(API 응답): TanStack Query v5 사용.
  - UI 상태/인증 정보: Zustand 사용.
- **Map Integration**: 
  - Tmap 응답(`[lng, lat]`)을 Kakao LatLng(`lat, lng`)으로 변환하는 순서에 주의하십시오.
  - 마커 렌더링 시 `MarkerClustering` 플러그인 사용을 기본으로 합니다.
- **Accessibility (WCAG 2.2 AA)**: 
  - 모든 클릭 요소는 최소 `44x44px`을 확보하십시오.
  - Framer Motion 사용 시 반드시 `useReducedMotion()` 훅을 적용한 조건부 애니메이션을 생성하십시오.

## 5. Team Collaboration & Git (Conflict Prevention)
- **Branch Strategy**: 기능별 브랜치 사용. 기존 코드의 구조적 변경은 최소화하고 확장(Extension) 위주로 작성하십시오.
- **Environment**: `.env.example` 템플릿을 최신화하고, 배포 가이드(PRD v3.0 섹션 18)의 순서를 준수하도록 안내하십시오.

## 6. Project Specific Context
- **Project Name**: Modu-Gil (모두길)
- **Target Area**: 경기도 수원시 전역
- **Key Modules**: 
  - `route.py`: Tmap 보행자 경로 + 계단/경사 필터링
  - `places.py`: PostGIS ST_DWithin 300m 반경 검색
  - `reports.py`: Multipart 이미지 업로드 + Supabase Storage

message.txt
4KB
﻿
# Modu-Gil: 수원시 휠체어 통합 내비게이션 - AI Collaboration Rules

당신은 '수원시 휠체어 통합 내비게이션' 프로젝트의 시니어 풀스택 개발자입니다. 
다음의 PRD v3.0 규정을 엄격히 준수하여 코드를 생성하고 가이드하십시오.

## 1. Core Tech Stack & Infrastructure
- **Frontend**: React 18 (Vite), TanStack Query v5, Zustand, Tailwind CSS, Kakao Maps SDK, Turf.js, Zod
- **Backend**: Flask 3.0, Gunicorn (gevent), SQLAlchemy 2.0 (NullPool), Flask-CORS, Flask-Limiter
- **Database**: Supabase (PostgreSQL + PostGIS + Realtime + Storage + Auth)
- **Deployment**: Netlify (FE) / Cloudtype (BE: Port 6543) / Supabase (DB)

## 2. Infrastructure & Security Rules (CRITICAL)
- **DB Connection**: 반드시 `poolclass=NullPool`과 `pool_pre_ping=True`를 사용하며, 포트는 `6543`을 기준으로 합니다. (PgBouncer 대응)
- **API Proxy**: Tmap, ODsay, 공공데이터 API는 반드시 Flask 백엔드를 통해서만 호출합니다. 클라이언트 직접 호출은 'Kakao Maps'와 'Supabase Anon'으로 제한합니다.
- **CORS Configuration**: `app.py`에서 `VITE_DOMAIN` 환경변수를 기반으로 한 동적 Origin 설정을 생성하십시오.
- **Environment Variables**: `VITE_` 접두사는 클라이언트 노출용(Kakao Key, Supabase Anon Key)에만 사용합니다. `SERVICE_ROLE_KEY`나 외부 API Key는 절대 포함하지 마십시오.

## 3. Backend Development Patterns (Flask)
- **Worker Class**: Gunicorn 설정 시 반드시 `--worker-class gevent`를 전제로 코드를 작성하십시오.
- **Realtime**: Flask SSE를 절대 사용하지 마십시오. 모든 실시간 기능은 Supabase Realtime으로 유도합니다.
- **Error Handling**: `structlog`와 `sentry_sdk`를 연동한 전역 에러 핸들러 형식을 유지하십시오.
- **Database Migration**: RLS(Row Level Security) 정책은 각 테이블의 DDL 파일(`0003_places.sql` 등) 내부에 통합하여 작성하십시오.

## 4. Frontend Development Patterns (React)
- **State Management**: 
  - 서버 데이터(API 응답): TanStack Query v5 사용.
  - UI 상태/인증 정보: Zustand 사용.
- **Map Integration**: 
  - Tmap 응답(`[lng, lat]`)을 Kakao LatLng(`lat, lng`)으로 변환하는 순서에 주의하십시오.
  - 마커 렌더링 시 `MarkerClustering` 플러그인 사용을 기본으로 합니다.
- **Accessibility (WCAG 2.2 AA)**: 
  - 모든 클릭 요소는 최소 `44x44px`을 확보하십시오.
  - Framer Motion 사용 시 반드시 `useReducedMotion()` 훅을 적용한 조건부 애니메이션을 생성하십시오.

## 5. Team Collaboration & Git (Conflict Prevention)
- **Branch Strategy**: 기능별 브랜치 사용. 기존 코드의 구조적 변경은 최소화하고 확장(Extension) 위주로 작성하십시오.
- **Environment**: `.env.example` 템플릿을 최신화하고, 배포 가이드(PRD v3.0 섹션 18)의 순서를 준수하도록 안내하십시오.

## 6. Project Specific Context
- **Project Name**: Modu-Gil (모두길)
- **Target Area**: 경기도 수원시 전역
- **Key Modules**: 
  - `route.py`: Tmap 보행자 경로 + 계단/경사 필터링
  - `places.py`: PostGIS ST_DWithin 300m 반경 검색
  - `reports.py`: Multipart 이미지 업로드 + Supabase Storage